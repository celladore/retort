<!-- GENERATED by AgentKit Forge v3.1.0 — DO NOT EDIT -->
<!-- Source: .agentkit/spec + .agentkit/overlays/agentkit-forge -->
<!-- Regenerate: pnpm -C .agentkit agentkit:sync -->

# ADR-02: Fallback Policy for Missing Evidence Metric

## Status

Proposed

## Date

2026-03-15

## Context

The `agentkit-forge` project uses one or more evidence-driven scoring or gating metrics (for example: cost evidence, telemetry confidence, quality signal confidence). In some workflows, required evidence can be missing at decision time.

Baseline source for this template: the current fallback ADR from `agentkit-forge`.

## Decision

When an evidence metric (e.g. cost evidence, telemetry confidence, quality signal confidence) is missing for an evaluated item:

1. Keep the current score or gate result unchanged.
2. Mark evidence status as `Not evaluated`.
3. Do not recalculate or adjust final outputs based only on missing evidence.

This fallback policy is approved by repository maintainers.

## Rationale

- Avoids penalising entities with incomplete evidence.
- Preserves scoring consistency until evidence can be collected.
- Sets an explicit timeline for evidence backfill.

## Scope

Applies to `/infra-eval` scoring dimensions, `/review` quality gates, and related evaluation workflows in `agentkit-forge`.
