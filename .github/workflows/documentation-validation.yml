# GENERATED by AgentKit Forge v3.1.0 — DO NOT EDIT
# Source: .agentkit/spec + .agentkit/overlays/agentkit-forge
# Regenerate: pnpm -C .agentkit agentkit:sync
name: Documentation Validation

on:
  pull_request:
    types: [opened, synchronize, ready_for_review]

jobs:
  documentation-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check documentation requirement
        id: doc-check
        env:
          GITHUB_BASE_REF: ${{ github.base_ref }}
        run: |
          chmod +x ./scripts/check-documentation-requirement.sh
          ./scripts/check-documentation-requirement.sh

      - name: Validate documentation structure
        run: |
          chmod +x ./scripts/validate-documentation.sh
          ./scripts/validate-documentation.sh

      - name: Validate sequential numbering
        run: |
          chmod +x ./scripts/validate-numbering.sh
          ./scripts/validate-numbering.sh
