{
  "name": "agentkit-forge-root",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "pnpm -C .agentkit test",
    "build": "pnpm -C .agentkit agentkit:validate",
    "lint:md": "pnpm -C .agentkit lint:md",
    "lint:md:fix": "pnpm -C .agentkit lint:md:fix"
  }
}
