<!-- GENERATED by AgentKit Forge v3.1.0 — DO NOT EDIT -->
<!-- Source: .agentkit/spec + .agentkit/overlays/agentkit-forge -->
<!-- Regenerate: pnpm -C .agentkit agentkit:sync -->
<!-- generated_by: agentkit-forge | last_model: sync-engine | last_updated: 2026-03-06 -->
<!-- Format: Plain Markdown. Windsurf command template. -->
<!-- Docs: https://docs.windsurf.com/windsurf/cascade -->

# /feature-review — Reviews the current feature configuration for the repo. Analyzes which features are enabled/disabled, checks for misconfigured dependencies, identifies unused features (enabled but no matching code patterns), and recommends features to enable based on codebase analysis.

## When to Use

Invoke this command when the user requests or implies the
`feature-review` operation.

## Purpose

Reviews the current feature configuration for the repo. Analyzes which features are enabled/disabled, checks for misconfigured dependencies, identifies unused features (enabled but no matching code patterns), and recommends features to enable based on codebase analysis.

## Shared State

This command participates in the shared workflow state. Read and update:

- **AGENT_BACKLOG.md** — Prioritized work items; read before starting, update when adding/completing tasks
- **.windsurf/state/orchestrator.json** — Phase, team status, metrics; read for context
- **.windsurf/state/events.log** — Append a log line when completing significant actions

## Flags

| Flag          | Description                                                                   | Default |
| ------------- | ----------------------------------------------------------------------------- | ------- |
| `--recommend` | Analyze codebase and recommend features to enable or disable                  | false   |
| `--audit`     | Audit feature usage — find enabled features with no matching code patterns    | false   |
| `--verbose`   | Show detailed feature metadata including template vars and affected templates | false   |

## Implementation

Execute the steps defined in the corresponding command (`.windsurf/commands/feature-review.md`). The full specification and allowed tools are in that file.

## Related Commands

- `/orchestrate` — Full lifecycle coordination (uses this command as a phase)
- `/plan` — Structured planning before implementation
- `/project-review` — Comprehensive project audit
- See `COMMAND_GUIDE.md` for when to choose each command
