---
description: "Security rules for AI-assisted development"
globs:
alwaysApply: true
generated_by: "{{lastAgent}}"
last_model: "{{lastModel}}"
last_updated: "{{syncDate}}"
# Format: MDC (Markdown with YAML frontmatter). Cursor rule file.
# Docs: https://docs.cursor.com/context/rules
---
# Security Rules
- Never read, print, or commit secrets or tokens
- Prefer least privilege and deny-by-default
- Never disable security checks to make tests pass
- Any auth change requires tests + docs
- Never write to .env, .tfvars, or credential files
