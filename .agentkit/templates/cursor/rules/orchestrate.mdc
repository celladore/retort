---
description: "Orchestration workflow for multi-agent coordination"
globs:
alwaysApply: false
generated_by: "{{lastAgent}}"
last_model: "{{lastModel}}"
last_updated: "{{syncDate}}"
# Format: MDC (Markdown with YAML frontmatter). Cursor rule file.
# Docs: https://docs.cursor.com/context/rules
---
# Orchestration

Follow the 5-phase lifecycle: Discovery → Planning → Implementation → Validation → Ship.

Teams: T1-BACKEND, T2-FRONTEND, T3-DATA, T4-INFRA, T5-DEVOPS, T6-TESTING, T7-SECURITY, T8-DOCS, T9-PRODUCT, T10-QUALITY.

Run .agentkit/bin/orchestrate for automated coordination.
